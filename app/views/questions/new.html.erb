<h1 class="mb-4 text-3xl font-bold" style="text-align: left;">New question for <%= @quiz.title %></h1>

<%= render "form", question: @question %>

<div class="text-left mt-6 mb-6">
<%= link_to 'Back to My Quizzes', quizzes_path, class: btn_back %>
</div>