<!DOCTYPE html>
<html>
  <head>
    <title>BrainBuster</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-white text-gray-800">
    <header class="bg-medium-blue p-4 shadow-md">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-3xl font-bold text-white">BrainBuster</h1>
        <% if user_signed_in? %>
          <div class="flex gap-4">
              <%= link_to "Profile", edit_user_registration_path, class: btn_class %>
              <%= link_to 'Take a Quiz', start_quiz_path, class: btn_class %>
              <%= link_to 'My Quizzes', quizzes_path, class: btn_class %>
          </div>
        <% else %>
          <div class="flex gap-4">
            <%= link_to "Sign Up", new_user_registration_path, class: btn_green %>
            <%= link_to "Log In", new_user_session_path, class: btn_class %>
          </div>
        <% end %>
      </div>
    </header>

    <div class="container mx-auto p-4">
      <%= render 'layouts/notice' %>
      <%= yield %>
    </div>
  </body>
</html>